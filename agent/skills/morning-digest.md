---
description: Use when the user wants a morning digest, a daily plan, or a summary of their open tasks.
---

# Утренний дайджест

Когда пользователь просит дайджест, сводку или план дня:

1. Вызови инструмент `tasks` с `action="list"`, чтобы получить актуальные незавершённые задачи.
2. Сгруппируй задачи:
   - **Просроченные / на сегодня** — у которых `dueAt` сегодня или раньше. Если `dueAt` отсутствует, не называй задачу просроченной: покажи её в остальных и предложи уточнить точный срок.
   - **Дедлайн в ближайшие 48 часов** — только с точным `dueAt` в ближайшие двое суток.
   - **Старты важных механик** — только задачи `priority="high"` с `startAt`; укажи, через сколько календарных дней старт.
   - **Высокий приоритет** — `priority="high"`, ещё не попавшие выше.
   - **Остальное** — всё прочее.
3. Сформируй короткую сводку обычным текстом (без Markdown-таблиц — Telegram их не рендерит):
   - Одна строка-приветствие.
   - По одному пункту на задачу: `• [id] текст — приоритет/срок`.
   - В конце — одно предложение с фокусом дня (что сделать в первую очередь).
4. Не упоминай новые сообщения рабочих чатов и не создавай задачи из чатов в этом дайджесте.
5. Если задач нет — скажи об этом коротко и предложи добавить первую.

Держи дайджест компактным: максимум 5–7 пунктов. Если задач больше — покажи самые важные
и упомяни, сколько ещё осталось.
